# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# Talos generated configs (contain secrets)
talos/*.yaml
talos/talosconfig
!talos/.gitkeep

# Nix
result

# SOPS/Age keys (never commit)
*.agekey
keys.txt
.config/sops/

# Kubeconfig (contains credentials)
kubeconfig
