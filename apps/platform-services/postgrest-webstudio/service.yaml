apiVersion: v1
kind: Service
metadata:
  name: postgrest-webstudio
  namespace: platform-services
spec:
  type: ClusterIP
  selector:
    app: postgrest-webstudio
  ports:
    - port: 3000
      targetPort: 3000
