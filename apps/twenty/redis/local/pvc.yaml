apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: twenty-redis-pvc
  namespace: twenty
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
