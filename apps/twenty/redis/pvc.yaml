apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: twenty-redis-pvc
  namespace: twenty
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      type: local
      app: twenty-redis
