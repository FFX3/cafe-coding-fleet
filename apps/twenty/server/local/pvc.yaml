apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: twenty-server-pvc
  namespace: twenty
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
