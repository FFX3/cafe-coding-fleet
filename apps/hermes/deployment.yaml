apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes
  namespace: hermes
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hermes
  template:
    metadata:
      labels:
        app: hermes
    spec:
      securityContext:
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: fix-permissions
          image: busybox:1.36
          command: ["sh", "-c", "chown -R 1000:1000 /data"]
          securityContext:
            runAsUser: 0
            runAsNonRoot: false
          volumeMounts:
            - name: hermes-data
              mountPath: /data
      containers:
        - name: dashboard
          image: nousresearch/hermes-agent:latest
          command: ["hermes", "dashboard", "--host", "0.0.0.0", "--port", "9119", "--insecure"]
          ports:
            - containerPort: 9119
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          envFrom:
            - configMapRef:
                name: hermes-config
            - secretRef:
                name: hermes-credentials
          volumeMounts:
            - name: hermes-data
              mountPath: /data
          resources:
            requests:
              memory: "256Mi"
              cpu: "50m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          startupProbe:
            tcpSocket:
              port: 9119
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            tcpSocket:
              port: 9119
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: 9119
            periodSeconds: 10
        - name: gateway
          image: nousresearch/hermes-agent:latest
          command: ["hermes", "gateway"]
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          envFrom:
            - configMapRef:
                name: hermes-config
            - secretRef:
                name: hermes-credentials
          env:
            - name: MATRIX_USER_ID
              valueFrom:
                secretKeyRef:
                  name: hermes-credentials
                  key: HERMES_MATRIX_USER_ID
            - name: MATRIX_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: hermes-credentials
                  key: HERMES_MATRIX_PASSWORD
          volumeMounts:
            - name: hermes-data
              mountPath: /data
          resources:
            requests:
              memory: "256Mi"
              cpu: "50m"
            limits:
              memory: "1Gi"
              cpu: "500m"
        - name: peitho-gateway
          image: nousresearch/hermes-agent:latest
          command: ["hermes", "gateway", "-p", "peitho"]
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          envFrom:
            - configMapRef:
                name: hermes-config
            - secretRef:
                name: hermes-credentials
          env:
            - name: MATRIX_USER_ID
              valueFrom:
                secretKeyRef:
                  name: hermes-credentials
                  key: PEITHO_MATRIX_USER_ID
            - name: MATRIX_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: hermes-credentials
                  key: PEITHO_MATRIX_PASSWORD
          volumeMounts:
            - name: hermes-data
              mountPath: /data
          resources:
            requests:
              memory: "256Mi"
              cpu: "50m"
            limits:
              memory: "1Gi"
              cpu: "500m"
      volumes:
        - name: hermes-data
          persistentVolumeClaim:
            claimName: hermes-pvc
