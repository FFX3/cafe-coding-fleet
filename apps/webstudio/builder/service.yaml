apiVersion: v1
kind: Service
metadata:
  name: webstudio-builder
  namespace: webstudio
spec:
  type: ClusterIP
  selector:
    app: webstudio-builder
  ports:
    - port: 3000
      targetPort: 3000
